import { EmptyState } from "@/components/ui/EmptyState";

export default function AdminWorkspacesPage() {
  return (
    <EmptyState title="Workspace management coming soon" description="Inspect and manage workspaces across the platform." />
  );
}
